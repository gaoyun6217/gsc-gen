// Code generated by gfrd-gen. DO NOT EDIT.
// Route registration for {{ .Table.Comment }}

package genrouter

import (
	"context"

	"github.com/gogf/gf/v2/net/ghttp"
	"{{ .Package }}/internal/controller/{{ .Module }}"
)

// Register{{ .EntityName }} 注册{{ .Table.Comment }}路由
func Register{{ .EntityName }}(ctx context.Context, group *ghttp.RouterGroup) {
	group.Group("/{{ .EntityKebab }}", func(group *ghttp.RouterGroup) {
{{- if .Features.list }}
		group.Bind({{ .Module }}.{{ .EntityName }}.List)
{{- end }}
{{- if .Features.view }}
		group.Bind({{ .Module }}.{{ .EntityName }}.View)
{{- end }}
{{- if .Features.add }}
		group.Bind({{ .Module }}.{{ .EntityName }}.Add)
{{- end }}
{{- if .Features.edit }}
		group.Bind({{ .Module }}.{{ .EntityName }}.Edit)
{{- end }}
{{- if .Features.delete }}
		group.Bind({{ .Module }}.{{ .EntityName }}.Delete)
{{- end }}
	})
}
